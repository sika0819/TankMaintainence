using UnityEngine;
using System.Collections;
namespace HoloToolkit.Unity
{
    public class test : MonoBehaviour
    {

        //public float SphereStartTime;
      //  private float StartTime = Time.time;
       // private pingyi yi = new pingyi();
       // private IEnumerator hh;
       // private float TimeStart;
       // private float TimeBetweenUpdates = 1f;
       // private float velocity=2f;
        //private float smoothTimecity = 0.001f;
        //private Vector3 MaxSpeed = new Vector3(1,1,1);
        // Use this for initialization
        void Start()
        {

            //Vector3 PosOrigin = transform.position;
           // float StartTime = Time.time;
           // TimeStart = StartTime;
           // SphereStartTime = StartTime;     
           // Debug.Log("fsgsg");
            //hh = yi.move(gameObject, PosOrigin, StartTime, TimeBetweenUpdates);
            // StartCoroutine(yi.move(gameObject, PosOrigin, StartTime, TimeBetweenUpdates));
          //  StartCoroutine(hh);
        }
       
        // Update is called once per frame
        void Update()
        {
            //  var Maxspeed = 1;
           // transform.position=new Vector3 ( Mathf.SmoothDamp(transform.position.x, GameObject.Find("Sphere").transform.position.x, ref velocity,smoothTimecity ,200), Mathf.SmoothDamp(transform.position.y, GameObject.Find("Sphere").transform.position.y, ref velocity, smoothTimecity, 200),transform.position.z) ;
           // transform.position = Vector3.Lerp(transform.position, GameObject.Find("Sphere").transform.position, 0.05f);
          //  if (Time.time - TimeStart > 7f)
             //   StopCoroutine(hh);
           // {
           //  Debug.Log("协程停止");
           //  StopCoroutine(yi.move(gameObject, PosOrigin, StartTime, TimeBetweenUpdates));
           //   Debug.Log("协程停止");
           // }
        }
    }
}