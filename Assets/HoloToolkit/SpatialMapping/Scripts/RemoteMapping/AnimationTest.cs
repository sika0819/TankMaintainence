using UnityEngine;
using System.Collections;

public class AnimationTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //  this.GetComponent <Animation >().Play("MyAnimation");
        // this.GetComponent<Animation>().Play("MyAnimation2");
        //Object ObjectVisual = Resources.Load("donghua", typeof(GameObject));
        //GameObject ObjectReal = Instantiate(ObjectVisual, Vector3 .one*20 , Quaternion.identity) as GameObject;

       // Object Object2 = Resources.Load("donghua", typeof(GameObject));
       // GameObject Object3 = Instantiate(ObjectVisual, Vector3.one*10, Quaternion.identity) as GameObject;
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
